# Attendance Mobile (Expo)

## Setup
1. Install dependencies:
   - `npm install`
2. Start the app:
   - `npm run start`

## Notes
- This is a mobile-first UI optimized for fast attendance marking.
- Biometric support is UI-ready and can be wired later with `expo-local-authentication`.
